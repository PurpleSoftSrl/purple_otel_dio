name: purple_otel_dio
description: OpenTelemetry auto-instrumentation for Dio HTTP client — automatic CLIENT spans with W3C trace context.
version: 0.1.0
repository: https://xamapps.visualstudio.com/AI/_git/purple_otel_dio
homepage: https://pub.dev/packages/purple_otel_dio
topics: [opentelemetry, dio, http, tracing, flutter]
license: AGPL-3.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  purple_otel_sdk:
    path: ../purple_otel_sdk
  dio: ^5.7.0

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
