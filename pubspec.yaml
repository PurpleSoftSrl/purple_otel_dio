name: purple_otel_dio
description: OpenTelemetry auto-instrumentation for Dio HTTP client — automatic CLIENT spans with W3C trace context.
version: 0.1.3
repository: https://github.com/purplesoftsrl/purple_otel_dio
homepage: https://github.com/purplesoftsrl/purple_otel_dio
topics: [opentelemetry, dio, http, tracing]
license: AGPL-3.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  purple_otel_sdk: ^0.1.0
  dio: ^5.7.0

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
