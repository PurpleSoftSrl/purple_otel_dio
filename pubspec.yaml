name: purple_otel_dio
description: OpenTelemetry auto-instrumentation for Dio HTTP client — auto-spans, trace context injection.
version: 0.1.0
license: Apache-2.0
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  purple_otel_sdk:
    path: ../purple_otel_sdk
  dio: ^5.7.0

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
